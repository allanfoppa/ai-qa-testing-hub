# NOTES

The most challenging aspect of the AI report is the missing appropriate selectors and naming conventions, such as 'data-testid', 'data-test' or 'data-qa', to unambiguously identify attributes intended for automation.

I need to research better generic selectors to cover multiple applications.

The logic is as follows: Scrap -> Select and extract -> Send to AI model -> Get AI response -> Generate report works well.
